[build]
  publish = "."

[build.environment]
  NODE_VERSION = "18"

[[redirects]]
  from  = "/signup"
  to    = "/signup.html"
  status = 200

[[redirects]]
  from  = "/complete-account"
  to    = "/pages/complete-account.html"
  status = 301

[[headers]]
  for = "/*"
  [headers.values]
    X-Frame-Options          = "DENY"
    X-Content-Type-Options   = "nosniff"
    Referrer-Policy          = "strict-origin-when-cross-origin"
    Permissions-Policy       = "camera=(self), microphone=()"

[[headers]]
  for = "/sw.js"
  [headers.values]
    Cache-Control = "no-cache"

[[headers]]
  for = "/icons/*"
  [headers.values]
    Cache-Control = "public, max-age=31536000, immutable"

[[headers]]
  for = "/js/*"
  [headers.values]
    Cache-Control = "public, max-age=86400"

# ═══════════════════════════════════════════════════════
# VARIABLES DE ENTORNO REQUERIDAS (configurar en Netlify)
# ═══════════════════════════════════════════════════════
# SUPABASE_URL          = https://egswsqymkxmbtcpnozcq.supabase.co
# SUPABASE_ANON_KEY     = eyJ... (clave pública)
# SUPABASE_SERVICE_KEY  = eyJ... (clave privada — solo en functions)
# OPENAI_API_KEY        = sk-... (para food-photo IA)
# HOTMART_WEBHOOK_TOKEN = token de validación de webhook Hotmart
